// Static-SPA: no server-side rendering, no prerendering. Everything runs in
// the browser so the Supabase client (wired by the human) can own all data
// access at runtime.
export const ssr = false;
export const prerender = false;
